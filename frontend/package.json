{
  "name": "icon-marketplace-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8080",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "vite": "^5.4.12"
  }
}
